- Confirmed ingestion-service builds and /health responds locally — [02/08/2026]
- Confirmed intersection-service builds and /health responds locally — [02/08/2026]
- Confirmed intersection-watchdog builds and /health responds locally — [02/08/2026]
- Confirmed routing-service builds and /health responds locally — [02/08/2026]
- Confirmed congestion-service builds and /health responds locally — [02/08/2026]
- Stage 1: implemented CSV cleaning in IngestionServiceApp (trim/case normalization, missing-value handling as explicit null, boolean flag normalization, duplicate collapsing by normalized ID with conflict logging) and exposed GET /intersections. Verified cleaning logic against the real intersections-legacy.csv with a standalone test harness — 18 raw rows correctly collapse to 17 records, matches README's worked example. Still need to confirm mvn package + the live GET /intersections endpoint on my own machine (Maven Central wasn't reachable in the dev sandbox this was drafted in). — [19/08/2026]
- Refactored: pulled the cleaning logic out of IngestionServiceApp into its own IntersectionCleaner class (still flat co.wethinkcode.trafficflow package, just a separate file) so it's unit-testable without spinning up Javalin. Added JUnit 5 + Surefire to pom.xml and wrote IntersectionCleanerTest with grouped tests per data issue (casing, padding/whitespace, missing-value placeholders, boolean normalization, duplicate merging incl. conflict resolution) plus two end-to-end tests against the real bundled CSV. 52 equivalent assertions verified via a manual harness (same Maven Central limitation as before) — real mvn test run still needed on my machine. — [19/08/2026]
